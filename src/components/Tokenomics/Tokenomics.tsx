import React from "react";
import { Section } from "../../layout";

const Tokenomics = () => {
  return (
    <Section id="tokenomics" className="h-[400px] my-10 bg-red-500">
      <div className="flex flex-col md:flex-row justify-center">
        <div className="text-2xl text-center m-auto">Tokenomics</div>
      </div>
    </Section>
  );
};

export default Tokenomics;
